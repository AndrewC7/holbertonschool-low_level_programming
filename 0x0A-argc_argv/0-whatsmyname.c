#include <stdio.h>

/**
 * main - entry point
 *
 * @argc: empty
 * @argv: array of strings passed to program
 *
 * Return: 0 (Success)
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
